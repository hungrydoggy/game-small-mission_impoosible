auto fsm = params[0].as<Fsm*>();  auto delta_time = params[2].as<double>();  
#line 42 "assets/objects/char.main/playable.obj.fsm"
          
          auto self = fsm->owner();
          auto move_vel = self->getData("move_vel", 0.0);
          self->x(math::max(self->x() - move_vel * delta_time, self->scene()->getData("bound.left", 0.0)));
          return null_var;
