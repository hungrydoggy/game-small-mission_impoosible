auto self = params[0].as<GameObject*>();  auto my_shape = params[1].as<BulletShape*>();  auto other_shape = params[2].as<BulletShape*>();  auto contact_info = params[3].as<void*>();  

auto other = dynamic_cast<GameObject*>(other_shape->body_owner());
auto is_player = other->getData("type", "") == "player";
unlikely (is_player == false)
  return null_var;

self->unregisterBody();
self->sendMessage(other, "incHp", {Var(1)});
self->setAction("disappear");
return null_var;
